class ChartManager {
    constructor () {
        
    }
    
    publishNewChart (metadata, chart_data, creator_email) {
        // Check project ID - if there is already a chart with this project id, then update it
        
        // Use metadata.thumbnails to get 2 thumbnails: one max res, one small res
        // Upload these thumbnails to the thumbs store under randomly generated names and store the links
        
        // Create JSON chart object with chart_data, metadata, and more
        
        // Use creator_email to get the creator's user_id - do not include email in the chart json
    }
}